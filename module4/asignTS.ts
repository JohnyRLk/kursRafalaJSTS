/*
import dataF from "./first.json";
import dataT from "./secend.json";
const concatJsons = function (to: object, from: object) {
  const jsonKeysF: string[] = Object.keys(from);
  const jsonKeysTo: string[] = Object.keys(to);
  const result: object = {};

  jsonKeysF.forEach((key) => (result[key] = from[key]));
  console.log(result);

  jsonKeysTo.forEach((key) => (result[key] = to[key]));
  console.log(result);
};
concatJsons(dataT, dataF);*/
